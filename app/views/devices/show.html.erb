<%= turbo_stream_from @device %>

<p id="notice"><%= notice %></p>

<%= render @device %>
<%= render "devices/comments", device: @device %>

<div>
  <%= link_to "Edit this device", edit_device_path(@device) %> |
  <%= link_to "Back to devices", devices_path %>

  <%= button_to "Destroy this device", device_path(@device), method: :delete %>
</div>
